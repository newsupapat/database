extends layout

block content
    h1 #{title}
    table(border = 1)
            th รหัส 
            th ชื่อหนังสือ
            th ราคา
            for item in data
                tr 
                    td #{item.Employeeid}
                    td #{item.Name[0].FullName} 
                    td #{item.gender}    